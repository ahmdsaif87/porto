---
import Section from "./Section.astro";
import type { StudyProps } from "@types";

interface Props {
  study: StudyProps[];
} 

const { study } = Astro.props;
---

<Section text="Education" href="study">
  {
    study.map(({ institution, degree, startDate, endDate }) => (
      <div class="mb-10">
        <h3 class="mb-1.5 font-serif text-2xl font-semibold text-white">
          {institution}
        </h3>
        <div class="flex flex-col items-start pb-5">
          <h4 class="mb-0.5 font-serif text-2xl font-medium text-primary">
            {degree}
          </h4>
          <span class="pb-[2px] text-sm text-white/70">
            {startDate} — {endDate}
          </span>
        </div>
      </div>
    ))
  }
</Section>